# v6.5.2 — Purchase flow & admin fixes

## فایل‌های تغییرکرده
bot.py, wallet.py, v63_handlers.py, admin.py, db.py
(بقیه فایل‌های پروژه دست‌نخورده‌ان، فقط همین ۵ تا رو رو گیت overwrite کن)

## باگ‌های رفع‌شده

1. **تحویل سرویس بدون QR/کپشن/دکمه (مسیر شارژ کیف‌پول)**
   در wallet.py، تابع cb_confirm (تأیید رسید شارژ توسط ادمین) به‌جای دو پیام جدا
   (متن بی‌دکمه + عکس بی‌کپشن)، حالا دقیقاً مثل مسیر خرید مستقیم یک پیام واحد
   می‌فرسته: عکس QR + کپشن کامل + دکمه‌های «راهنمای اتصال» و «تمدید همین سرویس».

2. **گم‌شدن اسم دلخواه سرویس در مسیر شارژ کیف‌پول**
   ستون جدید topups.target_custom_name اضافه شد (migration خودکار).
   اسم انتخابی مشتری الان تا لحظه‌ی تأیید رسید توسط ادمین هم نگه داشته میشه
   و به provision_provider_purchase پاس داده میشه.

3. **جابه‌جایی مرحله‌ی اسم دلخواه**
   دکمه‌ی «اسم دلخواه» از صفحه‌ی تأیید خرید حذف شد. حالا بعد از زدن
   «پرداخت و دریافت سرویس» (فقط برای پلن‌های provider-based، نه پول لینک‌ها)
   یه صفحه‌ی میانی میاد: «اسم دلخواه» یا «پیش‌فرض (رندوم)».

4. **عدم امکان حذف پلن (و در نتیجه دسته‌بندی)**
   تابع db.delete_plan اضافه شد + دکمه‌ی «🗑 حذف پلن» تو صفحه‌ی جزئیات پلن
   (با یه مرحله تأیید). اگه پلن سابقه‌ی فروش نداشته باشه کامل حذف میشه؛
   اگه داشته باشه فقط غیرفعال/آرشیو میشه تا تاریخچه‌ی فروش خراب نشه.
   پلن پیش‌فرض قابل حذف نیست.

## نکات فنی
- کد رفکتور شد: منطق پرداخت/تأمین/تحویل خرید مستقیم از buy_qty جدا شد
  تو تابع bot._finalize_purchase، تا هم مسیر «پیش‌فرض» و هم مسیر
  «اسم دلخواه» از همون کد استفاده کنن (بدون تکرار کد).
- همه‌ی فایل‌ها با py_compile سینتکسشون چک شده، مشکلی نداره.
- import کامل با aiogram اینجا تست نشد (نصب aiogram روی این محیط شکست خورد)،
  پس حتماً رو خود سرور یه تست دستی از هر ۴ مسیر بگیر:
  خرید با موجودی کافی / خرید نیازمند شارژ / حذف پلن بی‌سابقه / حذف پلن باسابقه.
